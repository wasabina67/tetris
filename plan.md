# Tetris プロジェクト分析・実装プラン

## プロジェクト構造分析

### 現状
- **技術スタック**: React 19.1.1 + TypeScript + Vite
- **開発環境**: ESLint設定済み、セミコロンなしのコーディング規則
- **ビルド設定**: 出力先は`docs`フォルダ（GitHub Pages対応）
- **現在の状態**: Viteデフォルトテンプレートのみ、テトリス実装なし

### ファイル構造
```
/home/wasabina67/dev/tetris/
├── src/
│   ├── App.tsx          # メインアプリコンポーネント
│   ├── App.css          # アプリスタイル
│   ├── index.css        # グローバルスタイル
│   ├── main.tsx         # エントリーポイント
│   └── assets/          # 静的リソース
├── package.json         # 依存関係・スクリプト
├── vite.config.ts       # Vite設定（ビルド先: docs）
└── tsconfig.json        # TypeScript設定
```

### 利用可能スクリプト
- `npm run dev` - 開発サーバー起動
- `npm run build` - 本番ビルド
- `npm run lint` - ESLintチェック

## Tetrisゲーム実装プラン

### 1. ゲームコアロジック実装

#### テトロミノ（テトリスピース）システム
- **7種類の基本形状定義**:
  - I（棒）, O（四角）, T（T字）, S（S字）, Z（Z字）, J（L逆）, L（L字）
- **テトロミノ操作**:
  - 4方向回転（時計回り・反時計回り）
  - 左右移動・高速落下
  - 壁・他ブロックとの衝突判定

#### ゲームボード管理
- **ボード仕様**: 20行×10列のグリッド
- **ブロック配置**: テトロミノの固定化処理
- **ライン消去**: 完全な横ライン検出・消去・スコア計算
- **ゲームオーバー**: 天井到達判定

### 2. ゲーム状態管理

#### Reactステート設計
```typescript
interface GameState {
  board: number[][]          // 20x10のゲームボード
  currentPiece: Tetromino    // 現在操作中のテトロミノ
  nextPiece: Tetromino       // 次のテトロミノ
  score: number              // 現在スコア
  level: number              // 現在レベル
  lines: number              // 消去ライン数
  gameStatus: 'playing' | 'paused' | 'gameOver' | 'menu'
}
```

#### カスタムフック設計
- `useGameState`: ゲーム全体の状態管理
- `useTetromino`: テトロミノの生成・操作・移動ロジック
- `useGameLoop`: ゲームタイマー・自動落下制御
- `useKeyboard`: キーボード入力ハンドリング

### 3. ユーザーインターフェース

#### メインゲーム画面コンポーネント
- `GameBoard`: メインのゲーム盤面表示
- `NextPiece`: 次のテトロミノプレビュー
- `GameInfo`: スコア・レベル・ライン数表示
- `GameControls`: ゲーム操作ボタン（ポーズ・リスタート）

#### 画面遷移
- スタート画面（タイトル・ゲーム開始）
- メインゲーム画面
- ポーズ画面
- ゲームオーバー画面

#### キーボード操作
- `←/→`: 左右移動
- `↓`: 高速落下
- `↑`: 回転
- `Space`: ドロップ
- `P`: ポーズ
- `R`: リスタート

### 4. スタイリング・ユーザーエクスペリエンス

#### CSS設計アプローチ
- **ゲームボード**: CSS Gridレイアウトでグリッド表現
- **テトロミノ**: 色分けされたブロック表示
- **ダークテーマ**: 既存のダークテーマを活用

#### ビジュアルエフェクト
- ライン消去時のアニメーション
- テトロミノ落下のスムーズな動き
- ゲームオーバー時のエフェクト
- レベルアップ時の視覚フィードバック

### 5. ゲーム機能・バランシング

#### 難易度システム
- **レベル進行**: 10ライン消去毎にレベルアップ
- **落下速度**: レベルに応じて段階的に高速化
- **スコアシステム**:
  - 単発ライン: 100pts × level
  - 2ライン同時: 300pts × level
  - 3ライン同時: 500pts × level
  - 4ライン同時(テトリス): 800pts × level

#### 品質保証
- TypeScript厳密型チェック
- ESLint規則遵守（セミコロンなし）
- エラーハンドリング
- パフォーマンス最適化

## 実装順序

### フェーズ1: 基盤構築
1. テトロミノ定義・基本データ構造
2. ゲームボード状態管理
3. 基本的なReactコンポーネント構造

### フェーズ2: コアゲームロジック
4. テトロミノ移動・回転・衝突判定
5. ライン消去・スコア計算
6. ゲームループ・自動落下

### フェーズ3: ユーザーインターフェース
7. キーボード操作ハンドリング
8. ゲーム画面UI実装
9. ゲーム状態遷移（スタート・ポーズ・ゲームオーバー）

### フェーズ4: スタイリング・体験向上
10. CSS実装・スタイリング
11. アニメーション・エフェクト追加
12. 最終調整・ポリッシュ

## 技術的考慮事項

- **パフォーマンス**: React.memo、useCallback活用で不要な再レンダリング防止
- **型安全性**: TypeScript strict modeで型エラー防止
- **コードスタイル**: CLAUDE.mdの規則（セミコロンなし）に準拠
- **ビルド最適化**: Vite本番ビルドでの最適化活用
